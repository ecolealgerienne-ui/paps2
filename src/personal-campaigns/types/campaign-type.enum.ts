/**
 * Campaign Type Enum
 * Mirrors the Prisma CampaignType enum
 */
export enum CampaignType {
  vaccination = 'vaccination',
  deworming = 'deworming',
  screening = 'screening',
  treatment = 'treatment',
  census = 'census',
  other = 'other',
}
