/**
 * Personal Campaign Status Enum
 * Mirrors the Prisma PersonalCampaignStatus enum
 */
export enum PersonalCampaignStatus {
  planned = 'planned',
  in_progress = 'in_progress',
  completed = 'completed',
  cancelled = 'cancelled',
}
